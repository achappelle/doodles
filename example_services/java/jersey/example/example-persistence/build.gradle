apply plugin: 'java'

task wrapper(type: Wrapper) {
    gradleVersion = '2.11'
}

repositories {
    mavenCentral()
}

dependencies {
    compile 'ch.qos.logback:logback-classic:1.1.3'
    compile 'javax.inject:javax.inject:latest.release'
}
